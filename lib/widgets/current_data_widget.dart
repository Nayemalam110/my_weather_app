import 'package:flutter/material.dart';

class CurrentDataWidget extends StatelessWidget {
  const CurrentDataWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
